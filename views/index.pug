extends layout

block content
  h1 Welcome to #{title}
  each message in messages
    ul
      li=message.text 
      li=message.user
      li=message.added
  button(onclick="location.href = '/new';") New Message